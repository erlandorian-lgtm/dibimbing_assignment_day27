DBT assignment Dibimbing:

Reflective Questions:

1. As far as I understand, dbt help us to cleaned and processed from raw data to data ready to serve (Transform). With step by step process from staging, intermediate and lastly is mart. From staging area, which help me to clean the raw data from Pagila. But before we're going into intermediate area, we should determine what kind of data we need for mart. For example, mart_customer_performance needed to answer average / lifetime_total_customer. Also understanding the grain is needed to determine this tables is suitable for customer / payment / film if we doing the group by if combining / joining several tables from staging area. In intermediate, I can do the aggregation, joining several tables into 1 table needed for several mart, making sure NULL data such as, total_rented (if the customers hasn't rented any film) become 0 using COALESCE(). After that, I can do the sql queries needed for mart to answer business questions without having to make complicated aggregation again. Also don't forget about .yml, which help us to double check the tables that we've already made. Whether the data has null / duplicate, accepted_range.minimal_value (dbt_utils) needed to avoid negative amount, unique_combination_of_columns if there isn't any unique identifier to differentiate the data so there isn't any duplicate columns. Lastly ensuring all the queries total data is matches with raw data and doesn't less / more with test. So for conclusion, even though we already build all the queries and made all the necessary precautions queries. Double check is needed to ensure the data that we serve are legit.

2. Actually a lot of bug happened when I made the assignment, such as dbt command isn't installed, queries have an error message when I do the dbt run and several things about the queries. With AI support, for example, I ask about the installment of dbt and tried it myself. Taking some notes if it's working and continue the assignment. When it comes to queries, after did the dbt run and if there's an error from the queries, tried to fix it but it constantly failed. I would ask AI if there is a solution for my queries.
